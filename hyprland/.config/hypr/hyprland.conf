# https://wiki.hyprland.org/Configuring/
# You can split this configuration into multiple files
# Create your files separately and then link them to this file like this:
# source = ~/.config/hypr/myColors.conf

##################
### WALLPAPERS ###
##################

$set_wp = ~/.config/hypr/scripts/setwallpaper_random.sh
exec = sleep 1 && $set_wp

################
### MONITORS ###
################

# See https://wiki.hyprland.org/Configuring/Monitors/
# Globale skjerminnstillinger
monitor=,preferred,auto,auto
# Innebygd skjerm
monitor=desc:BOE 0x0846,1920x1080@60,auto,1.2
# Dell monitor
monitor=desc:Dell Inc. DELL P2422H HBW0YP3,1920x1080@60,auto-up,1#,mirror,desc:BOE 0x0846
# Skjermen som sto utenfor huset
monitor=desc:Samsung Electric Company SyncMaster 0x4C523234,preferred,auto-right,1.0


###################
### MY PROGRAMS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/

# Set programs that you use
$terminal = ghostty
$fileManager = dolphin
$menu = rofi -show drun -show-icons -matching fuzzy


#################
### AUTOSTART ###
#################

# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at launch like this:

##env = WLR_KEYBOARD_LAYOUT,no #testing

exec-once = $terminal
exec-once = waybar & hyprpaper 
exec-once = swaync
exec-once = systemctl --user start hyprpolkitagent

exec = tmux setenv -g HYPRLAND_INSTANCE_SIGNATURE "$HYPRLAND_INSTANCE_SIGNATURE"

# exec-once = nm-applet &
# exec-once = waybar & hyprpaper & firefox


#############################
### ENVIRONMENT VARIABLES ###
#############################

# See https://wiki.hyprland.org/Configuring/Environment-variables/

env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24


###################
### PERMISSIONS ###
###################

# See https://wiki.hyprland.org/Configuring/Permissions/
# Please note permission changes here require a Hyprland restart and are not applied on-the-fly
# for security reasons

# ecosystem {
#   enforce_permissions = 1
# }

# permission = /usr/(bin|local/bin)/grim, screencopy, allow
# permission = /usr/(lib|libexec|lib64)/xdg-desktop-portal-hyprland, screencopy, allow
# permission = /usr/(bin|local/bin)/hyprpm, plugin, allow


#####################
### LOOK AND FEEL ###
#####################

# Refer to https://wiki.hyprland.org/Configuring/Variables/

# https://wiki.hyprland.org/Configuring/Variables/#general
general {
    gaps_in = 2
    gaps_out = 4

    border_size = 2

    # https://wiki.hyprland.org/Configuring/Variables/#variable-types for info about colors
    col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
    col.inactive_border = rgba(595959aa)

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = true

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false

    layout = dwindle
}

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    rounding = 1
    rounding_power = 4

    # Change transparency of focused and unfocused windows
    active_opacity = 1.0
    inactive_opacity = 1.0

    shadow {
        enabled = false
        range = 4
        render_power = 3
        color = rgba(1a1a1aee)
    }

    # https://wiki.hyprland.org/Configuring/Variables/#blur
    blur {
        enabled = true
        size = 3
        passes = 1

        vibrancy = 0.1696
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
    enabled = yes, please :)

    # Default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = easeOutQuint,0.23,1,0.32,1
    bezier = easeInOutCubic,0.65,0.05,0.36,1
    bezier = easeInCubic,0.65,0.35,1,1
    bezier = linear,0,0,1,1
    bezier = almostLinear,0.5,0.5,0.75,1.0
    bezier = quick,0.15,0,0.1,1
    bezier = waitboom,1,-0.4,1,-0.4


    animation = global, 1, 10, default
    animation = border, 1, 5.39, easeOutQuint
    animation = windows, 1, 4.79, easeOutQuint
    animation = windowsIn, 1, 4.1, easeOutQuint, popin 95%
    animation = windowsOut, 1, 1.49, linear, popin 95%
    animation = fadeIn, 1, 1.73, almostLinear
    animation = fadeOut, 1, 1.46, almostLinear
    animation = fade, 1, 3.03, quick
    animation = layers, 1, 3.81, easeOutQuint
    animation = layersIn, 1, 4, easeOutQuint, fade
    animation = layersOut, 1, 1.5, linear, fade
    animation = fadeLayersIn, 1, 1.6, easeInCubic
    animation = fadeLayersOut, 1, 2.0, almostLinear#waitboom
    animation = workspaces, 1, 1.31, almostLinear, slidefade 10%
    animation = workspacesIn, 1, 1.31, almostLinear, slidefade 10%
    animation = workspacesOut, 1, 1.31, almostLinear, slidefade 10%
}

# Ref https://wiki.hyprland.org/Configuring/Workspace-Rules/
# "Smart gaps" / "No gaps when only"
# uncomment all if you wish to use that.
# workspace = w[tv1], gapsout:0, gapsin:0
# workspace = f[1], gapsout:0, gapsin:0
# windowrule = bordersize 0, floating:0, onworkspace:w[tv1]
# windowrule = rounding 0, floating:0, onworkspace:w[tv1]
# windowrule = bordersize 0, floating:0, onworkspace:f[1]
# windowrule = rounding 0, floating:0, onworkspace:f[1]

# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to SHIFT + mainMod + F in the keybinds section below
    preserve_split = true # You probably want this
}

# See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
master {
    new_status = master
}

# https://wiki.hyprland.org/Configuring/Variables/#misc
misc {
    force_default_wallpaper = 0 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = true # If true disables the random hyprland logo / anime girl background. :(
}


#############
### INPUT ###
#############

# https://wiki.hyprland.org/Configuring/Variables/#input
input {
    kb_layout = no
    kb_variant =
    kb_model =
    kb_options = altwin:swap_lalt_lwin,ctrl:swapcaps

    kb_rules =

    follow_mouse = 1

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.

    touchpad {
        natural_scroll = true
        scroll_factor = 0.5
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#gestures
gesture = 3, horizontal, workspace

# # Example per-device config
# # See https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs for more
device {
    name = mosart-semi.-2.4g-keyboard-mouse-1
    sensitivity = -0.2
    accel_profile = adaptive
}

device {
    name = apple-wireless-keyboard
    kb_layout = no-mac
    kb_variant = mac_custom
    kb_options = ctrl:swapcaps,lv3:lalt_switch
}

###################
### KEYBINDINGS ###
###################
# See https://wiki.hyprland.org/Configuring/Binds/ for more

# See https://wiki.hyprland.org/Configuring/Keywords/
$mainMod = SUPER # Sets "Windows" key as main modifier

# App launching shortcuts
bind = $mainMod, I, exec, $terminal
bind = $mainMod, E, exec, $fileManager
bind = $mainMod, SPACE, exec, $menu

# "System action" shortcuts
bind = $mainMod SHIFT CTRL, Q , exit
bind = $mainMod, N, exec, swaync-client -t
# NOTE: SUPER C and SUPER V are reserved for copy/paste
# (Must be remapped for each individual app)
# TODO: Actually implement this shit

# Window management
bind = $mainMod, Q, killactive,
bind = $mainMod, F, fullscreen,
bind = $mainMod SHIFT, F, togglefloating,
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, S, togglesplit, # dwindle
bind = $mainMod, M, movewindow, mon:+1
# bind = $mainMod, M, exec, $set_wp
bind = $mainMod SHIFT, M, movecurrentworkspacetomonitor, +1
# bind = $mainMod SHIFT, M, exec, $set_wp

bind = , PRINT, exec, hyprshot -m window
bind = SHIFT, PRINT, exec, hyprshot -m region

# Move focus with mainMod + hjkl keys
bind = $mainMod, h, movefocus, l
bind = $mainMod, j, movefocus, d
bind = $mainMod, k, movefocus, u
bind = $mainMod, l, movefocus, r
# Move windows around with mainMod SHIFT + hjkl
bind = $mainMod SHIFT, h, movewindow, l
bind = $mainMod SHIFT, j, movewindow, d
bind = $mainMod SHIFT, k, movewindow, u
bind = $mainMod SHIFT, l, movewindow, r

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
# bind = $mainMod, 1, exec, $w1
bind = $mainMod, 2, workspace, 2
# bind = $mainMod, 2, exec, $w2
bind = $mainMod, 3, workspace, 3
# bind = $mainMod, 3, exec, $w3
bind = $mainMod, 4, workspace, 4
# bind = $mainMod, 4, exec, $w4
bind = $mainMod, 5, workspace, 5
# bind = $mainMod, 5, exec, $w5
bind = $mainMod, 6, workspace, 6
# bind = $mainMod, 6, exec, $w6
bind = $mainMod, 7, workspace, 7
# bind = $mainMod, 7, exec, $wX
bind = $mainMod, 8, workspace, 8
# bind = $mainMod, 8, exec, $wX
bind = $mainMod, 9, workspace, 9
# bind = $mainMod, 9, exec, $wX
bind = $mainMod, 0, workspace, 10
# bind = $mainMod, 0, exec, $wX
bind = $mainMod, right, workspace, +1
bind = $mainMod, left, workspace, -1

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
# bind = $mainMod SHIFT, 1, exec, $w1
bind = $mainMod SHIFT CTRL, 1, movetoworkspacesilent, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
# bind = $mainMod SHIFT, 2, exec, $w2
bind = $mainMod SHIFT CTRL, 2, movetoworkspacesilent, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
# bind = $mainMod SHIFT, 3, exec, $w3
bind = $mainMod SHIFT CTRL, 3, movetoworkspacesilent, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
# bind = $mainMod SHIFT, 4, exec, $w4
bind = $mainMod SHIFT CTRL, 4, movetoworkspacesilent, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
# bind = $mainMod SHIFT, 5, exec, $w5
bind = $mainMod SHIFT CTRL, 5, movetoworkspacesilent, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
# bind = $mainMod SHIFT, 6, exec, $w6
bind = $mainMod SHIFT CTRL, 6, movetoworkspacesilent, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
# bind = $mainMod SHIFT, 7, exec, $wX
bind = $mainMod SHIFT CTRL, 7, movetoworkspacesilent, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
# bind = $mainMod SHIFT, 8, exec, $wX
bind = $mainMod SHIFT CTRL, 8, movetoworkspacesilent, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
# bind = $mainMod SHIFT, 9, exec, $wX
bind = $mainMod SHIFT CTRL, 9, movetoworkspacesilent, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10
# bind = $mainMod SHIFT, 0, exec, $wX
bind = $mainMod SHIFT CTRL, 0, movetoworkspacesilent, 10

# Shift workspace to the right or left
bind = $mainMod SHIFT, left, exec, ~/.config/hypr/scripts/move_workspace_preserve_layout.sh left
bind = $mainMod SHIFT, right, exec, ~/.config/hypr/scripts/move_workspace_preserve_layout.sh right

# Example special workspace (scratchpad)
# bind = $mainMod, S, togglespecialworkspace, magic
# bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Laptop multimedia keys for volume and LCD brightness
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl -e4 -n2 set 5%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl -e4 -n2 set 5%-

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
# See https://wiki.hyprland.org/Configuring/Workspace-Rules/ for workspace rules

# Example windowrule
# windowrule = float,class:^(kitty)$,title:^(kitty)$

# Ignore maximize requests from apps. You'll probably like this.
windowrule = suppressevent maximize, class:.*

# Fix some dragging issues with XWayland
windowrule = nofocus,class:^$,title:^$,xwayland:1,floating:1,fullscreen:0,pinned:0
